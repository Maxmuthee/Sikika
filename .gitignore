# Secrets
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Local data / SQLite (added in Step 4)
*.db
*.sqlite3

# OS
.DS_Store
Thumbs.db

#node modules
**/node_modules/

# Build output is VERSION-CONTROLLED on purpose: the VPS deploys by pulling the
# repo, so `dashboard/sikika-app/dist/` must be committed. ALWAYS rebuild before
# committing UI changes: cd dashboard/sikika-app && npm run build